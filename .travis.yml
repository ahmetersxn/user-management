language: java
jdk:
  - oraclejdk9
install:
  ./mvnw install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script:
  ./mvnw test -B

deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_API_KEY
  on:
    branch: production
    app: user-management
