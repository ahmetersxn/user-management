language: java
jdk:
  - oraclejdk9
install:
  - mvn clean install
script:
  - mvn test

deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_API_KEY
  on:
    branch: production
    app: user-management
